[FFI]
; Enable FFI globally for php-vips (FFI-based binding)
; Required for jcupitt/vips package to work
ffi.enable = true

; PHP 8.3+ stack overflow detection can interfere with FFI callbacks
; php-vips executes FFI callbacks off main thread which confuses stack checks
zend.detect_stack_depth = 0
